import {IOperation} from "../operation";
export class SumOperation implements IOperation {
    public param1:number;
    public param2:number;

    execuute():number{
        const result = this.param1
    }
} 
